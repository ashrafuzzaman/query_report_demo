<%= form_for(@invoice) do |f| %>
  <% if @invoice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@invoice.errors.count, "error") %> prohibited this invoice from being saved:</h2>

      <ul>
      <% @invoice.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :total_paid %><br />
    <%= f.text_field :total_paid %>
  </div>
  <div class="field">
    <%= f.label :total_charged %><br />
    <%= f.text_field :total_charged %>
  </div>
  <div class="field">
    <%= f.label :paid %><br />
    <%= f.check_box :paid %>
  </div>
  <div class="field">
    <%= f.label :received_by %><br />
    <%= f.text_field :received_by %>
  </div>
  <div class="field">
    <%= f.label :invoiced_on %><br />
    <%= f.date_select :invoiced_on %>
  </div>
  <div class="field">
    <%= f.label :paid_on %><br />
    <%= f.date_select :paid_on %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
